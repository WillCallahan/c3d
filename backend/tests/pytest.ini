[pytest]
markers =
    e2e: End-to-end tests that invoke real AWS API
    slow: Slow running tests
    unit: Fast unit tests
    integration: Integration tests

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = 
    -v
    --tb=short
    --strict-markers
